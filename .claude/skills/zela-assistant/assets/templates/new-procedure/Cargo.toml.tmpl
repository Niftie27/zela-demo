# Generated from zela-assistant skill template.
# Verify workspace dep names/versions in ../../Cargo.toml.
# For procedures using Solana RPC + native tests, see references/compile-and-setup.md (§3 Solana-aware variant).

[package]
name    = "{{NAME}}"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
zela-std.workspace = true
serde.workspace    = true
log.workspace      = true

# Uncomment if the procedure needs native unit tests:
# [dev-dependencies]
# tokio      = { version = "1", features = ["full"] }
# env_logger = { version = "0.11" }

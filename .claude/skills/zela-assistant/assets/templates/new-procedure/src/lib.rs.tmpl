// Generated from zela-assistant skill template.
// Replace TODOs. See references/procedure-anatomy.md for richer patterns
// (RPC clients, cfg-gated trait impls, native unit tests).

use serde::{Deserialize, Serialize};
use zela_std::{CustomProcedure, RpcError, zela_custom_procedure};

pub struct {{NAME}};

#[derive(Deserialize, Debug)]
pub struct Input {
    // TODO: define request fields
}

#[derive(Serialize)]
pub struct Output {
    // TODO: define response fields
}

impl CustomProcedure for {{NAME}} {
    type Params = Input;
    type ErrorData = ();
    type SuccessData = Output;
    const LOG_MAX_LEVEL: log::LevelFilter = log::LevelFilter::Debug;

    async fn run(params: Self::Params) -> Result<Self::SuccessData, RpcError<Self::ErrorData>> {
        log::debug!("params: {params:?}");

        // TODO: implement procedure logic.
        // Return errors with:
        //   Err(RpcError { code: 400, message: "...".into(), data: None })

        todo!("implement {{NAME}}::run")
    }
}

zela_custom_procedure!({{NAME}});
